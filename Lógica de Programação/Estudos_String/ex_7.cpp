#include <stdio.h>
#include <string.h>

main(){
	char palavra[50];
	char palindrono[50];
	int i=0, palin = 1, cont=0, x;
	
	printf("escreva a palavra: ");
	gets(palavra);
	
	while(palavra[i]!='\0'){
		cont++;
		palavra[i++];
	}
	
	
}
